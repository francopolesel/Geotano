// ---------------------------------------------------------------------------
// Geotano — Shared constants
// ---------------------------------------------------------------------------

/** Default timer per question in milliseconds. */
export const DEFAULT_TIMER_MS = 15_000;

/** Default number of lives per game. */
export const DEFAULT_LIVES = 3;

/** Maximum streak before multiplier kicks in. */
export const STREAK_THRESHOLD = 3;

/** Base score per correct answer. */
export const BASE_SCORE = 100;

/** Streak multiplier applied after the threshold. */
export const STREAK_MULTIPLIER = 1.5;

/** Number of answer options per question. */
export const OPTIONS_COUNT = 4;

/** Default JWT expiry. */
export const JWT_EXPIRY = '7d';

/** API prefix. */
export const API_PREFIX = '/api';
